[Unit]
Description=ancs4linux Desktop Integration daemon (should run as user)
Requires=bluetooth.target

[Service]
Type=simple
ExecStart=/usr/local/bin/ancs4linux-desktop-integration

[Install]
WantedBy=default.target
